# Weather Forecast Web App

A simple weather forecast web application built with React + TypeScript.
The app retrieves current weather data using the OpenWeatherMap API, updates the UI based on weather conditions, and uses a custom caching mechanism to avoid unnecessary API calls for the same city for 10 minutes.
